# Screen 9 - Auth

Uses existing scholarly palette, typography, spacing rhythm, and border conventions.
Auth flow is integrated via shared shell action `login` and backend endpoint `/api/auth/login`.
